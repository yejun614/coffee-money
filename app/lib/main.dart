import "package:flutter/material.dart";

void main() {
  runApp(CoffeeMoneyApp());
}

class CoffeeMoneyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Text("Hello"),
      ),
    );
  }
}
